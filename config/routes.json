{
  "routes": [
    {
      "method": "POST",
      "path": "/initialize-websocket",
      "handler": "echo.websocketController.initializeWebSocket",
      "config": {
        "policies": []
      }
    }
  ]
}
